export enum QuranFont {
  MadaniV2 = "code_v2",
}

export enum MushafLines {
  FifteenLines = "15_lines",
  SixteenLines = "16_lines",
}

export enum QCFFontVersion {
  V2 = "v2",
}

import { ResolvedTheme } from "@/types/theme";

export type ThemeTypeVariant = ResolvedTheme;

